project: NeuralHamilton_v1.33_DeepONet
device: cuda:0
net: model.DeepONet
optimizer: splus.SPlus
scheduler: hyperbolic_lr.ExpHyperbolicLR
epochs: 250
batch_size: 100
seeds: [89, 231, 928, 1411, 2025]
data: util.load_more
criterion: util.LogCoshLoss
wandb: false
checkpoint_config:
  enabled: true
  save_every_n_epochs: 50
  keep_last_k: 2
  save_best: true
net_config:
  nodes: 512
  layers: 4
  branches: 32
optimizer_config:
  lr: 0.3547810970801674
  eps: 1.e-10
scheduler_config:
  upper_bound: 300
  max_iter: 250
  infimum_lr: 5.012313526454265e-05
